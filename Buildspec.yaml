version: 0.2
phases:
  install:
    commands:
      - pip install yq
      - pip install boto3
  pre_build:
    commands:
      - echo "Starting CliXX deployment"
      - echo "more commands"
  build:
    on-failure: ABORT
    commands:
      # - echo starting build on ⁠ date ⁠
      # - echo "Running Clixx Script"
      # - python create_vpc    
          deployment_flag="destroy"
          if [[ $deployment_flag == 'create' ]]
          then
              python destroy_CliXX.py
          elif [[ $deployment_flag == 'create' ]]
          then
              python create.clixx.py
          fi
  post_build:
    commands:
      - echo Build completed on ⁠ date ⁠
      #- echo "Running destroy CliXX Script"
      #- python destroy_clixx_and_vpc.py
